import React from 'react'
import { Form, TextArea } from 'semantic-ui-react'

const TextAreaExampleAutoHeight = () => (
  <Form>
    <TextArea autoHeight placeholder='Comments or concerns...' />
  </Form>
)

export default TextAreaExampleAutoHeight
